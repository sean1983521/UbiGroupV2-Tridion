/**
 * Type handling pack
 */

define(['./inline', './ajax', './image', './iframe'], function(inline, ajax, image, iframe){
    return {
        inline: inline,
        ajax: ajax,
        image: image,
        iframe: iframe
    };
})
